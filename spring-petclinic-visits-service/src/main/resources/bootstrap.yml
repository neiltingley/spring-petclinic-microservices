spring:

  cloud:
    config:
      activate:
        on-profile: dev
      uri: http://localhost:8888
  application:
    name: visits-service
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config-server:8080
