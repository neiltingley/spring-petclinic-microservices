spring:
  cloud:
    config:
      uri: http://localhost:8888
      activate:
        on-profile: dev
  application:
    name: discovery-server
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config-server:8080
