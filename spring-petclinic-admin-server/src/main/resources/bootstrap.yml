spring:
  config:
    activate:
      on-profile: dev
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: admin-server
---
spring:
  config:
    activate:
      on-profile: kube
  cloud:
    config:
      uri: http://config-server:8080
